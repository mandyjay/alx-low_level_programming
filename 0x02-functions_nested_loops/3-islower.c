#include "main.h"

/**
 * _islower - The checker for lowercase character
 * @c: character to be checked
 * Return: 1 for lowercase character or 0 for other thing else
 */

int _is_lower(int c)

{

	if (c >= 97 && c <= 122)
	{

	return (1);
	}

	return (0);
}
